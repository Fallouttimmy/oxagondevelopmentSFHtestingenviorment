{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/public",
  "public": true,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/:path*.html"
    }
  ]
}
